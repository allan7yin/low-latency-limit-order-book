#include "Limit.hpp"

class Book {
  private:
    Limit *buyTree;
    Limit *sellTree;
    Limit *stopSellTree;
    Limit *stopBuyTree;
    Limit *highestBuy;
    Limit *lowestSell;
    Limit *highestStopSell;
    Limit *lowestStopBuy;

    // methods
};